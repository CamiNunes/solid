namespace ocp.Problem
{
    public enum TipoRelatorio
    {
        PDF,    // Relatório em formato PDF
        HTML,   // Relatório em formato HTML
        CSV,    // Relatório em formato CSV (valores separados por vírgula)
        Excel,  // Relatório em formato Excel
        XML     // Relatório em formato XML
    }
}
